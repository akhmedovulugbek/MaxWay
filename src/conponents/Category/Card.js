/* import React from 'react'

const Card = ({ el }) => {
    return (
        <div>
            <button>
                {el.name}
            </button>
        </div>
    )
}

export default Card */
